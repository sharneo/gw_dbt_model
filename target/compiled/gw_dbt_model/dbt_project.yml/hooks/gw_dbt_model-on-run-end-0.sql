
  
    grant usage on schema L001_GWPC to role public;
    grant select on all tables in schema L001_GWPC to role public;
    grant select on all views in schema L001_GWPC to role public;
    grant select on future tables in schema L001_GWPC to role public;
    grant select on future views in schema L001_GWPC to role public;
  
