version: 2

models:
  - name: ai_pc_policy
    description: "Raw agency data loaded from S3"
    columns:
      - name: PUBLICID
        description: "Public ID of the C"
        tags: ['pii']
        tests:
          - not_null
      - name: accountid
        description: "Account ID"
        tags: ['pii']
        tests:
          - not_null
        meta:
          database_tags:
            pii: b